# Mission 1 | Add the repo to vela and trigger builds on tags and pushes

on pushes, we should tag a docker image with as in ~/github.com/zapplebee/velatest

on tags we should use that tag value to make a semantic version tag docker image
https://go-vela.github.io/docs/reference/environment/variables


You can read the current vela token from 001.secrets.md

then let's add steps to test the application.

stand up a simple webpage using https://github.com/authts/oidc-client-ts and vite.

then use a https://hub.docker.com/r/cypress/included/ image to run tests in the pipeline.

the best way to do this is to start the webpage as a vela service in the background.

https://go-vela.github.io/docs/usage/tour/services

then run the main app in pipeline after installing and building and detatching so you can move to the next step.

https://go-vela.github.io/docs/reference/yaml/steps#keys

do not ask me for any input until this is completed.